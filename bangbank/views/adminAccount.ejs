<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Account Applications - BangBank Admin</title>
  <style>
    /* Reset and base styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #e0e0e0;
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Header styles - matching userDashboard template */
    header {
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0, 255, 255, 0.2);
      padding: 1rem 2rem;
      box-shadow: 0 2px 20px rgba(0, 255, 255, 0.1);
      position: relative;
      z-index: 1000;
    }

    .header-content {
      max-width: 1500px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 2rem;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 700;
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
      flex-shrink: 0;
    }

    .welcome-message {
      font-size: 1.2rem;
      color: #ffffff;
      text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
      flex-grow: 1;
      text-align: center;
    }

    .nav-links {
      display: flex;
      gap: 1rem;
      flex-shrink: 0;
      flex-wrap: wrap;
    }

    .nav-links a {
      color: #e0e0e0;
      text-decoration: none;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      background: rgba(0, 255, 255, 0.1);
      border: 1px solid rgba(0, 255, 255, 0.3);
      transition: all 0.3s ease;
      font-weight: 500;
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
      font-size: 0.9rem;
    }

    .nav-links a:hover {
      color: #00ffff;
      background: rgba(0, 255, 255, 0.2);
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
      border-color: rgba(0, 255, 255, 0.5);
    }

.logout-btn {
      background: linear-gradient(135deg, rgb(229, 10, 10), #ff4444) !important;
      color: #ffffff !important;
      border: none !important;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(255, 0, 255, 0.3);
      letter-spacing: 0.5px;
      font-size: 0.85rem;
      text-decoration: none;
    }

    .logout-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 0, 255, 0.5);
      background: linear-gradient(135deg, #ff3300, #ff6666) !important;
    }

    /* Main content */
    main {
      flex: 1;
      padding: 2rem;
      max-width: 1400px;
      margin: 0 auto;
      width: 100%;
    }

    h1 {
      font-size: 2.5rem;
      color: #ffffff;
      font-weight: 300;
      margin-bottom: 2rem;
      text-align: center;
      text-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .no-applications {
      text-align: center;
      padding: 3rem;
      background: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(15px);
      border-radius: 20px;
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 8px 32px rgba(0, 255, 255, 0.2);
      color: rgba(224, 224, 224, 0.8);
      font-size: 1.1rem;
    }

    /* Table styles */
    .table-container {
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(15px);
      border-radius: 20px;
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 8px 32px rgba(0, 255, 255, 0.2);
      overflow: hidden;
      margin-bottom: 2rem;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      background: transparent;
    }

    th, td {
      padding: 1rem;
      text-align: left;
      border-bottom: 1px solid rgba(0, 255, 255, 0.2);
      vertical-align: middle;
    }

    th {
      background: rgba(0, 255, 255, 0.1);
      color: #00ffff;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-size: 0.85rem;
      text-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
      border-bottom: 2px solid rgba(0, 255, 255, 0.3);
    }

    tr:hover {
      background: rgba(0, 255, 255, 0.05);
    }

    tr:last-child td {
      border-bottom: none;
    }

    td {
      color: #e0e0e0;
      font-size: 0.9rem;
    }

    /* Action buttons */
    .action-buttons {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
    }

    .action-form {
      display: inline;
      margin: 0;
    }

    .approve-btn, .reject-btn {
      border: none;
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-size: 0.75rem;
    }

    .approve-btn {
      background: linear-gradient(135deg, #00ff88, #00cc66);
      color: #000000;
      box-shadow: 0 4px 15px rgba(0, 255, 136, 0.3);
    }

    .approve-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 255, 136, 0.5);
      background: linear-gradient(135deg, #44ffaa, #22dd77);
    }

    .reject-btn {
      background: linear-gradient(135deg, #ff4444, #cc0000);
      color: #ffffff;
      box-shadow: 0 4px 15px rgba(255, 68, 68, 0.3);
    }

    .reject-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 68, 68, 0.5);
      background: linear-gradient(135deg, #ff6666, #ee2222);
    }

    /* Document link */
    .document-link {
      color: #00ffff;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
      text-shadow: 0 0 5px rgba(0, 255, 255, 0.5);
    }

    .document-link:hover {
      color: #ff00ff;
      text-shadow: 0 0 10px rgba(255, 0, 255, 0.7);
    }

    /* Back navigation */
    .back-nav {
      margin-top: 2rem;
      text-align: center;
    }

    .back-nav a {
      text-decoration: none;
      color: #e0e0e0;
      font-weight: 500;
      padding: 0.75rem 2rem;
      border-radius: 25px;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      background: rgba(0, 255, 255, 0.1);
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
      display: inline-block;
    }

    .back-nav a:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
      background: rgba(0, 255, 255, 0.2);
      border-color: rgba(0, 255, 255, 0.5);
      color: #00ffff;
    }

    /* Responsive design */
    @media (max-width: 768px) {
      .header-content {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
      }

      .welcome-message {
        text-align: center;
      }

      .nav-links {
        flex-wrap: wrap;
        justify-content: center;
      }

      .nav-links a {
        padding: 0.5rem 1rem;
        font-size: 0.85rem;
      }

      main {
        padding: 1rem;
      }

      h1 {
        font-size: 2rem;
      }

      .table-container {
        overflow-x: auto;
      }

      table {
        min-width: 800px;
      }

      th, td {
        padding: 0.75rem 0.5rem;
        font-size: 0.8rem;
      }

      .action-buttons {
        flex-direction: column;
        gap: 0.25rem;
      }

      .approve-btn, .reject-btn {
        width: 100%;
        padding: 0.4rem 0.8rem;
      }
    }

    @media (max-width: 480px) {
      .nav-links {
        flex-direction: column;
        gap: 0.5rem;
      }

      .nav-links a {
        width: 100%;
        text-align: center;
      }

      h1 {
        font-size: 1.8rem;
      }
    }

    /* Background decoration */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: 
        radial-gradient(circle at 25% 25%, rgba(0, 255, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 75% 75%, rgba(255, 0, 255, 0.1) 0%, transparent 50%);
      pointer-events: none;
      z-index: -1;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    header, main {
      animation: fadeIn 0.6s ease-out;
    }

    main {
      animation-delay: 0.2s;
    }

    /* Focus states for accessibility */
    .nav-links a:focus,
    .approve-btn:focus,
    .reject-btn:focus,
    .logout-btn:focus,
    .back-nav a:focus {
      outline: 2px solid rgba(0, 255, 255, 0.5);
      outline-offset: 2px;
    }

    /* Smooth scrolling */
    html {
      scroll-behavior: smooth;
    }
  </style>
</head>

<body>
  <header>
    <div class="header-content">
      <div class="logo">BangBank</div>
      <div class="welcome-message">Welcome, <%= user.username %> (<%= user.role %>)</div>
      <nav class="nav-links">
        <a href="/admin/dashboard">Back to Dashboard</a>
        <a href="/admin/products">Manage Products</a>
        <a href="/admin/creditcards">Credit Card Applications</a>
        <a href="/profile">Profile Settings</a>
        <a href="/logout" class="logout-btn">Logout</a>
      </nav>
    </div>
  </header>

  <main>
    <h1>Pending Account Applications</h1>

    <% if (accounts.length === 0) { %>
      <div class="no-applications">
        <p>No pending account applications found.</p>
      </div>
    <% } else { %>
      <div class="table-container">
        <table>
          <thead>
            <tr>
              <th>User</th>
              <th>Email</th>
              <th>Product</th>
              <th>KYC Type</th>
              <th>ID Number</th>
              <th>Document</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% accounts.forEach(app => { %>
              <tr>
                <td><%= app.username %></td>
                <td><%= app.userEmail %></td>
                <td><%= app.product_name %></td>
                <td><%= app.id_type %></td>
                <td><%= app.id_number %></td>
                <td>
                  <a href="<%= app.document_path %>" target="_blank" class="document-link">View</a>
                </td>
                <td>
                  <div class="action-buttons">
                    <form action="/admin/accounts/approve/<%= app.account_id %>" method="POST" class="action-form">
                      <button type="submit" class="approve-btn">Approve</button>
                    </form>
                    <form action="/admin/accounts/reject/<%= app.account_id %>" method="POST" class="action-form">
                      <button type="submit" class="reject-btn">Reject</button>
                    </form>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>

  </main>
</body>

</html>
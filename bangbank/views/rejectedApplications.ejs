<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rejected Applications - BangBank</title>
  <style>
    /* Reset and base styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #e0e0e0;
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
      min-height: 100vh;
    }

    /* Header styles */
    header {
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0, 255, 255, 0.2);
      padding: 1rem 2rem;
      box-shadow: 0 2px 20px rgba(0, 255, 255, 0.1);
      position: relative;
      z-index: 1000;
    }

    .header-content {
      max-width: 1500px;
      margin: 0 auto;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 5rem;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 700;
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
      flex-shrink: 0;
    }

    .welcome-message {
      font-size: 1.2rem;
      color: #ffffff;
      text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
      flex-grow: 1;
      text-align: center;
    }

    .nav-links {
      display: flex;
      gap: 1rem;
      flex-shrink: 0;
    }

    .nav-links a {
      color: #e0e0e0;
      text-decoration: none;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      background: rgba(0, 255, 255, 0.1);
      border: 1px solid rgba(0, 255, 255, 0.3);
      transition: all 0.3s ease;
      font-weight: 500;
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
    }

    .nav-links a:hover {
      color: #00ffff;
      background: rgba(0, 255, 255, 0.2);
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
      border-color: rgba(0, 255, 255, 0.5);
    }

    /* Main container */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }

    /* Empty state */
    .empty-state {
      background: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(15px);
      padding: 3rem;
      border-radius: 20px;
      border: 1px solid rgba(255, 70, 70, 0.3);
      box-shadow: 0 8px 32px rgba(255, 70, 70, 0.2);
      text-align: center;
      margin: 2rem 0;
    }

    .empty-state h2 {
      color: #ffffff;
      font-size: 1.8rem;
      margin-bottom: 1rem;
      text-shadow: 0 0 10px rgba(255, 70, 70, 0.3);
    }

    .empty-state p {
      color: rgba(224, 224, 224, 0.8);
      font-size: 1.1rem;
      margin-bottom: 2rem;
    }

    /* Section headers */
    .section-header {
      color: #ff4646;
      font-size: 1.8rem;
      font-weight: 600;
      margin: 2rem 0 1rem 0;
      text-shadow: 0 0 10px rgba(255, 70, 70, 0.5);
      text-align: center;
      position: relative;
    }

    .section-header::after {
      content: '';
      position: absolute;
      bottom: -5px;
      left: 50%;
      transform: translateX(-50%);
      width: 100px;
      height: 2px;
      background: linear-gradient(90deg, transparent, #ff4646, transparent);
      box-shadow: 0 0 10px rgba(255, 70, 70, 0.5);
    }

    /* Table styles */
    .table-container {
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(10px);
      border-radius: 15px;
      border: 1px solid rgba(255, 70, 70, 0.3);
      box-shadow: 0 8px 25px rgba(255, 70, 70, 0.1);
      margin-bottom: 2rem;
      overflow: hidden;
    }

    table {
      width: 100%;
      border-collapse: collapse;
    }

    th {
      background: rgba(255, 70, 70, 0.1);
      color: #ff4646;
      padding: 1rem;
      text-align: left;
      font-weight: 600;
      text-shadow: 0 0 5px rgba(255, 70, 70, 0.3);
      border-bottom: 1px solid rgba(255, 70, 70, 0.2);
    }

    td {
      padding: 1rem;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      color: #e0e0e0;
    }

    tr:hover {
      background: rgba(255, 70, 70, 0.05);
    }

    tr:last-child td {
      border-bottom: none;
    }

    /* Status indicators */
    .status-rejected {
      color: #ff4646;
      font-weight: 600;
      text-shadow: 0 0 5px rgba(255, 70, 70, 0.5);
      background: rgba(255, 70, 70, 0.1);
      padding: 0.25rem 0.5rem;
      border-radius: 10px;
      border: 1px solid rgba(255, 70, 70, 0.3);
      text-transform: uppercase;
      font-size: 0.8rem;
      letter-spacing: 0.5px;
    }

    /* Button styles */
    .btn {
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 25px;
      font-weight: 600;
      font-size: 0.9rem;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
      text-align: center;
      position: relative;
      overflow: hidden;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s;
    }

    .btn:hover::before {
      left: 100%;
    }

    .btn-primary {
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      color: #000000;
      box-shadow: 0 6px 20px rgba(0, 255, 255, 0.4);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 255, 255, 0.6);
    }

    .btn-secondary {
      background: rgba(100, 100, 255, 0.1);
      color: #6464ff;
      border: 1px solid rgba(100, 100, 255, 0.3);
      box-shadow: 0 4px 15px rgba(100, 100, 255, 0.2);
    }

    .btn-secondary:hover {
      background: rgba(100, 100, 255, 0.2);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(100, 100, 255, 0.4);
    }

    /* Responsive design */
    @media (max-width: 768px) {
      .header-content {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
      }

      .welcome-message {
        text-align: center;
      }

      .nav-links {
        flex-wrap: wrap;
        justify-content: center;
      }

      .nav-links a {
        padding: 0.5rem 1rem;
        font-size: 0.9rem;
      }

      .container {
        padding: 1rem;
      }

      .table-container {
        overflow-x: auto;
      }

      table {
        min-width: 600px;
      }
    }

    /* Background decoration */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: 
        radial-gradient(circle at 25% 25%, rgba(255, 70, 70, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 75% 75%, rgba(0, 255, 255, 0.05) 0%, transparent 50%);
      pointer-events: none;
      z-index: -1;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    } 

    .container > * {
      animation: fadeIn 0.6s ease-out;
    }

    @keyframes pulse {
      0% {
        box-shadow: 0 0 0 0 rgba(255, 70, 70, 0.4);
      }
      70% {
        box-shadow: 0 0 0 10px rgba(255, 70, 70, 0);
      }
      100% {
        box-shadow: 0 0 0 0 rgba(255, 70, 70, 0);
      }
    }

    .status-rejected {
      animation: pulse 2s infinite;
    }

    /* Focus states for accessibility */
    .btn:focus,
    .nav-links a:focus {
      outline: 2px solid rgba(255, 70, 70, 0.5);
      outline-offset: 2px;
    }

    /* Page title */
    .page-title {
      text-align: center;
      margin: 2rem 0;
      color: #ffffff;
      font-size: 2.2rem;
      font-weight: 700;
      text-shadow: 0 0 20px rgba(255, 70, 70, 0.3);
      background: linear-gradient(135deg, #ff4646, #ff8888);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* Info box */
    .info-box {
      background: rgba(255, 70, 70, 0.1);
      border: 1px solid rgba(255, 70, 70, 0.3);
      color: #ff8888;
      padding: 1.5rem;
      border-radius: 15px;
      margin: 2rem 0;
      text-align: center;
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 15px rgba(255, 70, 70, 0.1);
    }

    .info-box h3 {
      color: #ff4646;
      margin-bottom: 0.5rem;
      font-size: 1.2rem;
    }
  </style>
</head>

<body>
  <header>
    <div class="header-content">
      <div class="logo">BangBank</div>
      <div class="welcome-message">Hi, <%= user.username %>! View your rejected applications below.</div>
      <nav class="nav-links">
        <a href="/user/dashboard">Back to Dashboard</a>
        <a href="/profile">Profile Settings</a>
        <a href="/logout">Logout</a>
      </nav>
    </div>
  </header>

  <div class="container">
    <h1 class="page-title">Rejected Applications</h1>
    
    <div class="info-box">
      <h3>Application Status</h3>
      <p>These applications were not approved. You can apply for new products or contact our support team for more information about why your applications were rejected.</p>
    </div>

    <h2 class="section-header">Rejected Bank Accounts</h2>
    <% if (rejectedAccounts.length === 0) { %>
      <div class="empty-state">
        <h2>No Rejected Bank Accounts</h2>
        <p>You currently have no rejected account applications.</p>
        <a href="/customer/apply" class="btn btn-secondary">Apply for New Account</a>
      </div>
    <% } else { %>
      <div class="table-container">
        <table>
          <thead>
            <tr>
              <th>Account Name</th>
              <th>Product Type</th>
              <th>Initial Balance</th>
              <th>Status</th>
              <th>Applied On</th>
            </tr>
          </thead>
          <tbody>
            <% rejectedAccounts.forEach(account => { %>
              <tr>
                <td><%= account.account_type %></td>
                <td><%= account.product_type.replace('_',' ') %></td>
                <td>$<%= parseFloat(account.balance).toFixed(2) %></td>
                <td><span class="status-rejected"><%= account.status %></span></td>
                <td><%= new Date(account.opened_at).toDateString() %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>

    <h2 class="section-header">Rejected Credit Cards</h2>
    <% if (rejectedCards.length === 0) { %>
      <div class="empty-state">
        <h2>No Rejected Credit Cards</h2>
        <p>You currently have no rejected credit card applications.</p>
        <a href="/customer/apply" class="btn btn-secondary">Apply for New Card</a>
      </div>
    <% } else { %>
      <div class="table-container">
        <table>
          <thead>
            <tr>
              <th>Card Name</th>
              <th>Card Number</th>
              <th>Credit Limit</th>
              <th>Outstanding Balance</th>
              <th>Status</th>
              <th>Applied On</th>
            </tr>
          </thead>
          <tbody>
            <% rejectedCards.forEach(card => { %>
              <tr>
                <td><%= card.product_name %></td>
                <td><%= card.card_number %></td>
                <td>$<%= parseFloat(card.credit_limit).toFixed(2) %></td>
                <td>$<%= parseFloat(card.outstanding_balance).toFixed(2) %></td>
                <td><span class="status-rejected"><%= card.status %></span></td>
                <td><%= new Date(card.created_at).toDateString() %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>

    <div style="text-align: center; margin: 3rem 0;">
      <p style="color: #e0e0e0; font-size: 1.1rem; margin-bottom: 1.5rem;">Don't let rejection stop you! Explore our other banking products.</p>
      <a href="/customer/apply" class="btn btn-primary">View Available Products</a>
      <a href="/customer/consultations" class="btn btn-secondary" style="margin-left: 1rem;">Book Consultation</a>
    </div>
  </div>
</body>
</html>
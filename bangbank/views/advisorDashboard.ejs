<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Advisor Dashboard - BangBank</title>
  <style>
    /* Reset and base styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #e0e0e0;
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Background decoration */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: 
        radial-gradient(circle at 25% 25%, rgba(0, 255, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 75% 75%, rgba(255, 0, 255, 0.1) 0%, transparent 50%);
      pointer-events: none;
      z-index: -1;
    }

    /* Header styles */
    header {
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0, 255, 255, 0.2);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 20px rgba(0, 255, 255, 0.1);
      position: relative;
      z-index: 1000;
    }

    header h1 {
      font-size: 1.8rem;
      font-weight: 700;
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      letter-spacing: -0.5px;
      text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
    }

    .header-actions {
      display: flex;
      gap: 1rem;
      align-items: center;
    }

    .profile-link {
      text-decoration: none;
      color: #e0e0e0;
      font-weight: 500;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      transition: all 0.3s ease;
      background: rgba(0, 255, 255, 0.1);
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
    }

    .profile-link:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
      background: rgba(0, 255, 255, 0.2);
      border-color: rgba(0, 255, 255, 0.5);
      color: #00ffff;
    }

    .logout-btn {
      background: linear-gradient(135deg, #ff0066, #ff6600);
      color: #ffffff;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(255, 0, 102, 0.3);
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 0.9rem;
    }

    .logout-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(255, 0, 102, 0.5);
    }

    /* Main content */
    main {
      flex: 1;
      padding: 2rem;
      max-width: 1200px;
      margin: 0 auto;
      width: 100%;
    }

    /* Message styles */
    .message {
      padding: 1rem 1.5rem;
      border-radius: 10px;
      margin-bottom: 1.5rem;
      font-weight: 500;
      backdrop-filter: blur(10px);
      border: 1px solid;
      animation: fadeIn 0.5s ease-out;
    }

    .message.success {
      background: rgba(0, 255, 0, 0.1);
      border-color: rgba(0, 255, 0, 0.3);
      color: #00ff88;
      box-shadow: 0 4px 15px rgba(0, 255, 0, 0.1);
    }

    .message.error {
      background: rgba(255, 0, 0, 0.1);
      border-color: rgba(255, 0, 0, 0.3);
      color: #ff6666;
      box-shadow: 0 4px 15px rgba(255, 0, 0, 0.1);
    }

    /* Section styles */
    section {
      background: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(15px);
      border: 1px solid rgba(0, 255, 255, 0.3);
      border-radius: 20px;
      padding: 2rem;
      margin-bottom: 2rem;
      box-shadow: 0 8px 32px rgba(0, 255, 255, 0.1);
      position: relative;
      overflow: hidden;
    }

    section::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(0, 255, 255, 0.05), transparent);
      transition: left 0.5s;
    }

    section:hover::before {
      left: 100%;
    }

    section h2 {
      color: #ffffff;
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
      position: relative;
      z-index: 1;
    }

    /* Form styles */
    .create-session {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      align-items: end;
    }

    label {
      display: block;
      color: #00ffff;
      font-weight: 500;
      margin-bottom: 0.5rem;
      text-shadow: 0 0 5px rgba(0, 255, 255, 0.3);
    }

    input[type="date"],
    select {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 1px solid rgba(0, 255, 255, 0.3);
      border-radius: 10px;
      background: rgba(0, 0, 0, 0.7);
      color: #e0e0e0;
      font-size: 1rem;
      transition: all 0.3s ease;
      backdrop-filter: blur(5px);
    }

    input[type="date"]:focus,
    select:focus {
      outline: none;
      border-color: rgba(0, 255, 255, 0.6);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
      background: rgba(0, 0, 0, 0.8);
    }

    /* Style the calendar icon */
    input[type="date"]::-webkit-calendar-picker-indicator {
      filter: invert(1);
    }

    /* Button styles */
    .btn {
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
      text-align: center;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
      position: relative;
      overflow: hidden;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s;
    }

    .btn:hover::before {
      left: 100%;
    }

    .btn.primary {
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      color: #000000;
      font-weight: 700;
      box-shadow: 0 6px 20px rgba(0, 255, 255, 0.4);
    }

    .btn.primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 30px rgba(0, 255, 255, 0.6);
    }

    .btn.success {
      background: linear-gradient(135deg, #00ff88, #00cc66);
      color: #000000;
      font-weight: 700;
      box-shadow: 0 4px 15px rgba(0, 255, 136, 0.3);
    }

    .btn.success:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 255, 136, 0.5);
    }

    .btn.danger {
      background: linear-gradient(135deg, #ff0066, #cc0052);
      color: #ffffff;
      font-weight: 700;
      box-shadow: 0 4px 15px rgba(255, 0, 102, 0.3);
    }

    .btn.danger:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 0, 102, 0.5);
    }

    /* Table styles */
    .table-container {
      overflow-x: auto;
      border-radius: 15px;
      background: rgba(0, 0, 0, 0.4);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(0, 255, 255, 0.2);
      box-shadow: 0 4px 15px rgba(0, 255, 255, 0.1);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.9rem;
    }

    thead {
      background: rgba(0, 255, 255, 0.1);
      border-bottom: 2px solid rgba(0, 255, 255, 0.3);
    }

    th {
      padding: 1rem;
      text-align: left;
      font-weight: 600;
      color: #00ffff;
      text-shadow: 0 0 5px rgba(0, 255, 255, 0.3);
      border-bottom: 1px solid rgba(0, 255, 255, 0.2);
    }

    td {
      padding: 1rem;
      border-bottom: 1px solid rgba(0, 255, 255, 0.1);
      color: #e0e0e0;
    }

    tr:hover {
      background: rgba(0, 255, 255, 0.05);
    }

    /* Status badges */
    .completed {
      background: rgba(0, 255, 0, 0.2);
      color: #00ff88;
      padding: 0.25rem 0.75rem;
      border-radius: 15px;
      font-size: 0.8rem;
      font-weight: 600;
      border: 1px solid rgba(0, 255, 0, 0.3);
      box-shadow: 0 0 10px rgba(0, 255, 0, 0.2);
    }

    .cancelled {
      background: rgba(255, 0, 0, 0.2);
      color: #ff6666;
      padding: 0.25rem 0.75rem;
      border-radius: 15px;
      font-size: 0.8rem;
      font-weight: 600;
      border: 1px solid rgba(255, 0, 0, 0.3);
      box-shadow: 0 0 10px rgba(255, 0, 0, 0.2);
    }

    .booked {
      background: rgba(0, 255, 255, 0.2);
      color: #00ffff;
      padding: 0.25rem 0.75rem;
      border-radius: 15px;
      font-size: 0.8rem;
      font-weight: 600;
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
    }

    /* Action buttons */
    .action-buttons {
      display: flex;
      gap: 0.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .inline-form {
      display: inline;
    }

    /* Textarea styles */
    textarea {
      width: 100%;
      min-height: 60px;
      padding: 0.5rem;
      border: 1px solid rgba(0, 255, 255, 0.3);
      border-radius: 8px;
      background: rgba(0, 0, 0, 0.7);
      color: #e0e0e0;
      font-family: inherit;
      font-size: 0.9rem;
      resize: vertical;
      transition: all 0.3s ease;
      backdrop-filter: blur(5px);
    }

    textarea:focus {
      outline: none;
      border-color: rgba(0, 255, 255, 0.6);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
      background: rgba(0, 0, 0, 0.8);
    }

    /* Empty state */
    .empty-state {
      text-align: center;
      padding: 3rem 2rem;
      color: rgba(224, 224, 224, 0.7);
      font-size: 1.1rem;
      background: rgba(0, 0, 0, 0.3);
      border-radius: 15px;
      border: 1px solid rgba(0, 255, 255, 0.2);
      backdrop-filter: blur(10px);
    }

    .empty-state p {
      margin-bottom: 1rem;
    }

    .empty-state::before {
      content: '📋';
      display: block;
      font-size: 3rem;
      margin-bottom: 1rem;
      filter: drop-shadow(0 0 10px rgba(0, 255, 255, 0.3));
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateX(-20px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    header {
      animation: slideIn 0.6s ease-out;
    }

    main {
      animation: fadeIn 0.8s ease-out;
      animation-delay: 0.2s;
    }

    section {
      animation: fadeIn 0.8s ease-out;
    }

    section:nth-child(2) { animation-delay: 0.1s; }
    section:nth-child(3) { animation-delay: 0.2s; }
    section:nth-child(4) { animation-delay: 0.3s; }

    /* Responsive design */
    @media (max-width: 768px) {
      header {
        flex-direction: column;
        gap: 1rem;
        padding: 1rem;
      }
      
      header h1 {
        font-size: 1.5rem;
      }
      
      .header-actions {
        width: 100%;
        justify-content: center;
      }
      
      main {
        padding: 1rem;
      }
      
      section {
        padding: 1.5rem;
      }
      
      .create-session {
        grid-template-columns: 1fr;
      }
      
      .action-buttons {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .btn {
        width: 100%;
        text-align: center;
      }
    }

    @media (max-width: 480px) {
      header {
        padding: 0.5rem;
      }
      
      .header-actions {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .profile-link,
      .logout-btn {
        width: 100%;
        text-align: center;
      }
      
      section {
        padding: 1rem;
      }
      
      .table-container {
        font-size: 0.8rem;
      }
      
      th, td {
        padding: 0.5rem;
      }
    }

    /* Focus states for accessibility */
    .btn:focus,
    .profile-link:focus,
    input:focus,
    select:focus,
    textarea:focus {
      outline: 2px solid rgba(0, 255, 255, 0.5);
      outline-offset: 2px;
    }

    /* Smooth scrolling */
    html {
      scroll-behavior: smooth;
    }

    /* Loading states */
    .loading {
      opacity: 0.7;
      pointer-events: none;
    }

    .loading::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 20px;
      height: 20px;
      border: 2px solid rgba(0, 255, 255, 0.3);
      border-top: 2px solid #00ffff;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      transform: translate(-50%, -50%);
    }

    @keyframes spin {
      0% { transform: translate(-50%, -50%) rotate(0deg); }
      100% { transform: translate(-50%, -50%) rotate(360deg); }
    }
  </style>
</head>

<body>
  <header>
    <h1>Welcome, <%= user.username %> (Advisor)</h1>
    <div class="header-actions">
      <a href="/profile" class="profile-link">Profile Settings</a>
      <form action="/logout" method="GET">
        <button type="submit" class="logout-btn">Logout</button>
      </form>
    </div>
  </header>

  <main>
    <!-- Success / Error Messages -->
    <% if (typeof success !== 'undefined' && success) { %>
      <div class="message success">
        <%= success %>
      </div>
    <% } %>
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="message error">
        <%= error %>
      </div>
    <% } %>

    <section>
      <h2>Create New Consultation Session</h2>
      <form class="create-session" action="/advisor/sessions/create" method="POST">
        <div>
          <label for="session_date">Date:</label>
          <input type="date" name="session_date" id="session_date" min="<%= new Date().toISOString().split('T')[0] %>" required>
        </div>

        <div>
          <label for="start_time">Start Time:</label>
          <select name="start_time" id="start_time" required>
            <option value="">Select Time</option>
            <option value="09:00">09:00 AM</option>
            <option value="10:00">10:00 AM</option>
            <option value="11:00">11:00 AM</option>
            <option value="12:00">12:00 PM</option>
            <option value="13:00">01:00 PM</option>
            <option value="14:00">02:00 PM</option>
            <option value="15:00">03:00 PM</option>
            <option value="16:00">04:00 PM</option>
            <option value="17:00">05:00 PM</option>
          </select>
        </div>

        <div>
          <button type="submit" class="btn primary">Create Session</button>
        </div>
      </form>
    </section>

    <section>
      <h2>Your Created Sessions</h2>
      <% if (typeof mySessions !== 'undefined' && mySessions.length > 0) { %>
        <div class="table-container">
          <table>
            <thead>
              <tr>
                <th>Date</th>
                <th>Start Time</th>
                <th>End Time</th>
                <th>Booked Count</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% mySessions.forEach(s => { %>
                <tr>
                  <td><%= s.session_date %></td>
                  <td><%= s.session_time %></td>
                  <td><%= s.end_time %></td>
                  <td><%= s.booking_count %></td>
                  <td>
                    <div class="action-buttons">
                      <a href="/advisor/sessions/edit/<%= s.session_id %>" class="btn success">Edit</a>
                      <form class="inline-form" action="/advisor/sessions/delete/<%= s.session_id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this session?');">
                        <button type="submit" class="btn danger">Delete</button>
                      </form>
                    </div>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div class="empty-state">
          <p>You have no available sessions at the moment.</p>
          <p>Create your first session to get started with consultations.</p>
        </div>
      <% } %>
    </section>

    <section>
      <h2>Your Booked Consultations</h2>
      <% if (consultations.length === 0) { %>
        <div class="empty-state">
          <p>No consultations booked yet.</p>
          <p>Your scheduled consultations will appear here once customers book your sessions.</p>
        </div>
      <% } else { %>
        <div class="table-container">
          <table>
            <thead>
              <tr>
                <th>Customer Name</th>
                <th>Customer Email</th>
                <th>Date</th>
                <th>Time</th>
                <th>Status</th>
                <th>Notes</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% consultations.forEach(c => { %>
                <tr>
                  <td><%= c.customer_name %></td>
                  <td><%= c.customer_email %></td>
                  <td><%= c.session_date %></td>
                  <td><%= c.session_time %></td>
                  <td>
                    <% if (c.status === 'completed') { %>
                      <span class="completed">Completed</span>
                    <% } else if (c.status === 'cancelled') { %>
                      <span class="cancelled">Cancelled</span>
                    <% } else { %>
                      <span class="booked">Booked</span>
                    <% } %>
                  </td>
                  <td>
                    <% if (c.status === 'booked') { %>
                      <form action="/advisor/consultations/notes/<%= c.consultation_id %>" method="POST">
                        <textarea name="notes" placeholder="Add consultation notes..."><%= c.notes || '' %></textarea>
                        <button type="submit" class="btn primary" style="margin-top: 0.5rem;">Save Notes</button>
                      </form>
                    <% } else { %>
                      <div style="padding: 0.5rem; background: rgba(0, 255, 255, 0.1); border-radius: 5px; min-height: 40px;">
                        <%= c.notes ? c.notes : 'No notes added' %>
                      </div>
                    <% } %>
                  </td>
                  <td>
                    <% if (c.status === 'booked') { %>
                      <form action="/advisor/consultations/complete/<%= c.consultation_id %>" method="POST">
                        <button type="submit" class="btn success">Mark as Completed</button>
                      </form>
                    <% } else { %>
                      <span style="color: rgba(224, 224, 224, 0.5);">-</span>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      <% } %>
    </section>
  </main>

  <script>
    // Add some interactive enhancements
    document.addEventListener('DOMContentLoaded', function() {
      // Add loading state to forms
      const forms = document.querySelectorAll('form');
      forms.forEach(form => {
        form.addEventListener('submit', function() {
          const submitBtn = form.querySelector('button[type="submit"]');
          if (submitBtn) {
            submitBtn.style.opacity = '0.7';
            submitBtn.style.pointerEvents = 'none';
            submitBtn.innerHTML = 'Processing...';
          }
        });
      });

      // Add smooth transitions to table rows
      const tableRows = document.querySelectorAll('tbody tr');
      tableRows.forEach((row, index) => {
        row.style.animation = `fadeIn 0.5s ease-out ${index * 0.1}s both`;
      });

      // Auto-resize textareas
      const textareas = document.querySelectorAll('textarea');
      textareas.forEach(textarea => {
        textarea.addEventListener('input', function() {
          this.style.height = 'auto';
          this.style.height = this.scrollHeight + 'px';
        });
      });
    });
  </script>
</body>

</html>
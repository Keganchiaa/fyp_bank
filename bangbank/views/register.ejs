<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BangBank - Register</title>
  <style>
    /* Reset and base styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #e0e0e0;
      background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      padding-top: 120px; /* Account for header height */
    }

    /* Header styles */
    .header {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0, 255, 255, 0.2);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 20px rgba(0, 255, 255, 0.1);
      z-index: 1000;
    }

    .header h1 {
      font-size: 2rem;
      font-weight: 700;
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      letter-spacing: -0.5px;
      text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
    }

    .nav-links {
      display: flex;
      gap: 1rem;
    }

    .nav-links a {
      text-decoration: none;
      color: #e0e0e0;
      font-weight: 500;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      background: rgba(0, 255, 255, 0.1);
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
    }

    .nav-links a:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
      background: rgba(0, 255, 255, 0.2);
      border-color: rgba(0, 255, 255, 0.5);
      color: #00ffff;
    }

    /* Registration container */
    .registration-container {
      background: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(15px);
      padding: 3rem;
      border-radius: 20px;
      border: 1px solid rgba(0, 255, 255, 0.3);
      box-shadow: 0 8px 32px rgba(0, 255, 255, 0.2);
      max-width: 600px;
      width: 100%;
      position: relative;
      overflow: hidden;
      margin-bottom: 2rem;
    }

    .registration-container::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(0, 255, 255, 0.1), transparent);
      transition: left 0.5s;
    }

    .registration-container:hover::before {
      left: 100%;
    }

    .registration-title {
      font-size: 3rem;
      color: #ffffff;
      font-weight: 700;
      margin-bottom: 0.5rem;
      text-align: center;
      background: linear-gradient(135deg, #00ffff 0%, #ff00ff 50%, #00ffff 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 0 30px rgba(0, 255, 255, 0.6);
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    .registration-subtitle {
      font-size: 1rem;
      color: rgba(224, 224, 224, 0.7);
      text-align: center;
      margin-bottom: 2.5rem;
      font-weight: 300;
    }

    /* Alert messages */
    .alert {
      padding: 1rem;
      border-radius: 10px;
      margin-bottom: 1.5rem;
      text-align: center;
      font-weight: 500;
      position: relative;
      overflow: hidden;
    }

    .alert::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
      transition: left 0.5s;
    }

    .alert:hover::before {
      left: 100%;
    }

    .alert-error {
      background: rgba(220, 53, 69, 0.2);
      border: 1px solid rgba(220, 53, 69, 0.4);
      color: #ff6b6b;
      box-shadow: 0 0 15px rgba(220, 53, 69, 0.3);
    }

    /* Form styles */
    .registration-form {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    /*here*/.form-section {
      border: 1px solid rgba(0, 255, 255, 0.2);
      border-radius: 15px;
      padding: 2rem;
      background: rgba(0, 0, 0, 0.3);
      backdrop-filter: blur(5px);
      position: relative;
    }

    .form-section h3 {
      color: #00ffff;
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
      border-bottom: 2px solid rgba(0, 255, 255, 0.3);
      padding-bottom: 0.5rem;
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }

    .form-row.single {
      grid-template-columns: 1fr;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .form-group label {
      color: #00ffff;
      font-weight: 500;
      font-size: 0.95rem;
      text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
    }

    .form-group input,
    .form-group select {
      padding: 1rem;
      border: 1px solid rgba(0, 255, 255, 0.3);
      border-radius: 10px;
      background: rgba(0, 0, 0, 0.5);
      color: #e0e0e0;
      font-size: 1rem;
      transition: all 0.3s ease;
      backdrop-filter: blur(5px);
    }

.form-group input:focus,
.form-group select:focus {
  outline: none;
  border-color: rgba(0, 255, 255, 0.6);
  box-shadow: 0 0 15px rgba(0, 255, 255, 0.3);
  background: linear-gradient(135deg, #1e2a44, #243b5a); /* lighter than black but still sleek */
}


    .form-group input::placeholder {
      color: rgba(224, 224, 224, 0.4);
    }

    .form-group select option {
      background: #1a1a2e;
      color: #e0e0e0;
    }

    /* File input styling */
    .file-input-wrapper {
      position: relative;
      overflow: hidden;
      display: inline-block;
      width: 100%;
    }

    .file-input-wrapper input[type=file] {
      position: absolute;
      left: -9999px;
    }

    .file-input-label {
      padding: 1rem;
      border: 1px solid rgba(0, 255, 255, 0.3);
      border-radius: 10px;
      background: rgba(0, 0, 0, 0.5);
      color: #e0e0e0;
      cursor: pointer;
      transition: all 0.3s ease;
      display: block;
      text-align: center;
      backdrop-filter: blur(5px);
    }

    .file-input-label:hover {
      border-color: rgba(0, 255, 255, 0.6);
      box-shadow: 0 0 15px rgba(0, 255, 255, 0.3);
      background: rgba(0, 0, 0, 0.7);
    }

    /* Button styles */
    .registration-btn {
      padding: 1.2rem;
      border: none;
      border-radius: 30px;
      background: linear-gradient(135deg, #00ffff, #ff00ff);
      color: #000000;
      font-weight: 700;
      font-size: 1rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      box-shadow: 0 6px 20px rgba(0, 255, 255, 0.4);
      margin-top: 1rem;
    }

    .registration-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 0, 255, 0.3), transparent);
      transition: left 0.5s;
    }

    .registration-btn:hover::before {
      left: 100%;
    }

    .registration-btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 30px rgba(0, 255, 255, 0.6);
    }

    .registration-btn:active {
      transform: translateY(-1px);
    }

    /* Additional links */
    .registration-links {
      text-align: center;
      margin-top: 1.5rem;
      padding-top: 1.5rem;
      border-top: 1px solid rgba(0, 255, 255, 0.2);
    }

    .registration-links a {
      color: rgba(224, 224, 224, 0.8);
      text-decoration: none;
      font-size: 0.9rem;
      transition: all 0.3s ease;
    }

    .registration-links a:hover {
      color: #00ffff;
      text-shadow: 0 0 5px rgba(0, 255, 255, 0.5);
    }

    /* Responsive design */
    @media (max-width: 768px) {
      .header {
        flex-direction: column;
        gap: 1rem;
        padding: 1rem;
      }
      
      .header h1 {
        font-size: 1.5rem;
      }
      
      .nav-links {
        width: 100%;
        justify-content: center;
      }
      
      .nav-links a {
        flex: 1;
        text-align: center;
        padding: 0.5rem 1rem;
      }
      
      .registration-container {
        padding: 2rem;
        margin: 1rem;
      }

      .registration-title {
        font-size: 2rem;
      }

      .form-row {
        grid-template-columns: 1fr;
      }

      .form-section {
        padding: 1.5rem;
      }

      body {
        padding: 1rem;
        padding-top: 140px; /* Adjust for mobile header */
      }
    }

    @media (max-width: 480px) {
      .nav-links {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .nav-links a {
        width: 100%;
      }
      
      .registration-container {
        padding: 1.5rem;
      }

      .registration-title {
        font-size: 2.2rem;
      }

      body {
        padding-top: 180px; /* More space for mobile stacked header */
      }
    }

    /* Background decoration */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: 
        radial-gradient(circle at 25% 25%, rgba(252, 252, 252, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 75% 75%, rgba(57, 2, 57, 0.1) 0%, transparent 50%);
      pointer-events: none;
      z-index: -1;
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .header,
    .registration-container {
      animation: fadeIn 0.6s ease-out;
    }

    .registration-container {
      animation-delay: 0.2s;
    }

    /* Focus states for accessibility */
    .nav-links a:focus,
    .registration-btn:focus,
    .form-group input:focus,
    .form-group select:focus {
      outline: 2px solid rgba(0, 255, 255, 0.5);
      outline-offset: 2px;
    }

    /* Smooth scrolling */
    html {
      scroll-behavior: smooth;
    }

    /* Fix calendar icon visibility */
input[type="date"]::-webkit-calendar-picker-indicator {
  filter: invert(1);
}

/* Fix password eye icon visibility */
.toggle-password {
  filter: invert(1);
}

/* Ensure equal width of form inputs */
.form-group input,
.form-group select {
  width: 100%;

/* Match input styling to theme */
.form-group input[type="file"] {
  padding: 1rem;
  border: 1px solid rgba(0, 255, 255, 0.3);
  border-radius: 10px;
  background: rgba(0, 0, 0, 0.5);
  color: #e0e0e0;
  font-size: 1rem;
  transition: all 0.3s ease;
  width: 100%;
  backdrop-filter: blur(5px);
}

/* Optional hover effect for file input */
.form-group input[type="file"]:hover {
  border-color: rgba(0, 255, 255, 0.6);
  box-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
  background: rgba(0, 0, 0, 0.7);
}

/* Label styling */
.form-group label {
  color: #00ffff;
  font-weight: 500;
  font-size: 0.95rem;
  margin-bottom: 0.5rem;
  display: block;
  text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
}


} 
  </style>
</head>
<body>
  <header class="header">
    <h1>BangBank</h1>
    <nav class="nav-links">
      <a href="/">Home</a>
      <a href="/login">Login</a>
    </nav>
  </header>

  <div class="registration-container">
    <h2 class="registration-title">Register</h2>
    <p class="registration-subtitle">Create your secure banking account</p>

    <!-- Show error message if exists -->
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert-error"><%= error %></div>
    <% } %>

    <form class="registration-form" action="/register" method="POST" enctype="multipart/form-data">
      <!-- Account Details Section -->
      <div class="form-section">
        <h3>Account Details</h3>
        <div class="form-row">
          <div class="form-group">
            <label for="username">Username</label>
            <input type="text" name="username" id="username" placeholder="Enter your username" required>
          </div>
          <div class="form-group">
            <label for="userEmail">Email Address</label>
            <input type="email" name="userEmail" id="userEmail" placeholder="Enter your email" required>
          </div>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label for="phone_number">Phone Number</label>
            <input type="tel" name="phone_number" id="phone_number" placeholder="Enter your phone number" required>
          </div>
          <div class="form-group">
            <label for="userPassword">Password</label>
            <input type="password" name="userPassword" id="userPassword" placeholder="Create a password" required>
          </div>
        </div>
        <div class="form-row single">
          <div class="form-group">
            <label for="confirmPassword">Confirm Password</label>
            <input type="password" name="confirmPassword" id="confirmPassword" placeholder="Confirm your password" required>
          </div>
        </div>
      </div>

      <!-- Personal Details Section -->
      <div class="form-section">
        <h3>Personal Information</h3>
        <div class="form-row">
          <div class="form-group">
            <label for="first_name">First Name</label>
            <input type="text" name="first_name" id="first_name" placeholder="Enter your first name" required>
          </div>
          <div class="form-group">
            <label for="last_name">Last Name</label>
            <input type="text" name="last_name" id="last_name" placeholder="Enter your last name" required>
          </div>
        </div>
        <div class="form-row">
          <div class="form-group">
            <label for="alias">Alias (Optional)</label>
            <input type="text" name="alias" id="alias" placeholder="Enter an alias">
          </div>
          <div class="form-group">
            <label for="date_of_birth">Date of Birth</label>
            <input type="date" name="date_of_birth" id="date_of_birth" required>
          </div>
        </div>
      </div>

      <!-- Address Section -->
      <div class="form-section">
        <h3>Address Information</h3>
        <div class="form-row">
          <div class="form-group">
            <label for="country">Country</label>
            <select name="country" id="country" required>
              <option value="" disabled selected>Select your country</option>
              <option value="Singapore">Singapore</option>
              <option value="Malaysia">Malaysia</option>
              <option value="Indonesia">Indonesia</option>
              <option value="Thailand">Thailand</option>
            </select>
          </div>
          <div class="form-group">
            <label for="postcode">Postal Code</label>
            <input type="text" name="postcode" id="postcode" placeholder="Enter postal code" required pattern="\d{6}" title="Postal code must be exactly 6 digits">
          </div>
        </div>
        <div class="form-row single">
          <div class="form-group">
            <label for="address_line_1">Address Line 1</label>
            <input type="text" name="address_line_1" id="address_line_1" placeholder="Enter your address" required>
          </div>
        </div>
        <div class="form-row single">
          <div class="form-group">
            <label for="address_line_2">Address Line 2 (Optional)</label>
            <input type="text" name="address_line_2" id="address_line_2" placeholder="Apartment, suite, etc.">
          </div>
        </div>
      </div>

<!-- Image Upload -->
<div class="form-group">
  <label for="userImage">Profile Image (Optional):</label>
  <input type="file" name="userImage" id="userImage" accept="image/*">
</div>

<button type="submit" class="registration-btn">Register</button>

    </form>

    <div class="registration-links">
      <a href="/login">Already have an account? Login here</a>
    </div>
  </div>
</body>
</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BangBank - Admin Dashboard</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #e0e0e0;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            min-height: 100vh;
        }

        /* Header styles - Updated to match user dashboard */
        header {
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 255, 255, 0.2);
            padding: 1rem 2rem;
            box-shadow: 0 2px 20px rgba(0, 255, 255, 0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .header-content {
            max-width: 1500px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 2rem;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(135deg, #00ffff, #ff00ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
            flex-shrink: 0;
        }

        .welcome-message {
            font-size: 1.2rem;
            color: #ffffff;
            text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
            flex-grow: 1;
            text-align: center;
        }

        .nav-links {
            display: flex;
            gap: 1rem;
            flex-shrink: 0;
            align-items: center;
        }

        .nav-links a {
            color: #e0e0e0;
            text-decoration: none;
            padding: 0.75rem 1.5rem;
            border-radius: 25px;
            background: rgba(0, 255, 255, 0.1);
            border: 1px solid rgba(0, 255, 255, 0.3);
            transition: all 0.3s ease;
            font-weight: 500;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
            white-space: nowrap;
        }

        .nav-links a:hover {
            color: #00ffff;
            background: rgba(0, 255, 255, 0.2);
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
            border-color: rgba(0, 255, 255, 0.5);
        }

.logout-btn {
      background: linear-gradient(135deg, rgb(229, 10, 10), #ff4444) !important;
      color: #ffffff !important;
      border: none !important;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(255, 0, 255, 0.3);
      letter-spacing: 0.5px;
      font-size: 0.85rem;
      text-decoration: none;
    }

    .logout-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 0, 255, 0.5);
      background: linear-gradient(135deg, #ff3300, #ff6666) !important;
    }

        /* Container styles */
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Messages styles */
        .message {
            padding: 1rem 2rem;
            margin: 1rem 0;
            border-radius: 10px;
            backdrop-filter: blur(10px);
            border: 1px solid;
            animation: fadeIn 0.5s ease-out;
        }

        .message.success {
            background: rgba(0, 255, 0, 0.1);
            border-color: rgba(0, 255, 0, 0.3);
            color: #00ff00;
            box-shadow: 0 4px 15px rgba(0, 255, 0, 0.2);
        }

        .message.error {
            background: rgba(255, 0, 0, 0.1);
            border-color: rgba(255, 0, 0, 0.3);
            color: #ff4444;
            box-shadow: 0 4px 15px rgba(255, 0, 0, 0.2);
        }

        /* Section styles */
        .section {
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(15px);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(0, 255, 255, 0.3);
            box-shadow: 0 8px 32px rgba(0, 255, 255, 0.2);
            margin-bottom: 2rem;
            position: relative;
            overflow: hidden;
        }

        .section::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 255, 0.05), transparent);
            transition: left 0.5s;
        }

        .section:hover::before {
            left: 100%;
        }

        .section h2 {
            color: #ffffff;
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            text-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
            background: linear-gradient(135deg, #00ffff, #ff00ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Form styles */
        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group label {
            color: #00ffff;
            font-weight: 600;
            margin-bottom: 0.5rem;
            text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
        }

        .form-group input,
        .form-group select {
            background: rgba(0, 0, 0, 0.8);
            border: 1px solid rgba(0, 255, 255, 0.3);
            border-radius: 10px;
            padding: 0.8rem 1rem;
            color: #e0e0e0;
            font-size: 1rem;
            transition: all 0.3s ease;
            box-shadow: 0 2px 10px rgba(0, 255, 255, 0.1);
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: rgba(0, 255, 255, 0.8);
            box-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
            background: rgba(0, 0, 0, 0.9);
        }

        .form-group input::placeholder {
            color: rgba(224, 224, 224, 0.5);
        }

        .submit-btn {
            background: linear-gradient(135deg, #00ffff, #ff00ff);
            color: #000000;
            border: none;
            padding: 1rem 2rem;
            border-radius: 25px;
            font-weight: 700;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 6px 20px rgba(0, 255, 255, 0.4);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(0, 255, 255, 0.6);
        }

        /* Table styles */
        .table-container {
            overflow-x: auto;
            border-radius: 15px;
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 255, 255, 0.3);
            box-shadow: 0 8px 32px rgba(0, 255, 255, 0.2);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            min-width: 600px;
        }

        th {
            background: rgba(0, 255, 255, 0.1);
            color: #00ffff;
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            text-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
            border-bottom: 2px solid rgba(0, 255, 255, 0.3);
        }

        td {
            padding: 1rem;
            border-bottom: 1px solid rgba(0, 255, 255, 0.1);
            color: #e0e0e0;
            transition: all 0.3s ease;
        }

        tr:hover td {
            background: rgba(0, 255, 255, 0.05);
            color: #ffffff;
        }

        /* Action buttons */
        .action-btn {
            display: inline-block;
            padding: 0.4rem 0.8rem;
            margin: 0 0.2rem;
            border-radius: 15px;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            text-align: center;
            border: 1px solid;
        }

        .action-btn.details {
            background: rgba(0, 255, 255, 0.1);
            border-color: rgba(0, 255, 255, 0.3);
            color: #00ffff;
        }

        .action-btn.details:hover {
            background: rgba(0, 255, 255, 0.2);
            box-shadow: 0 4px 15px rgba(0, 255, 255, 0.3);
        }

        .action-btn.edit {
            background: rgba(255, 255, 0, 0.1);
            border-color: rgba(255, 255, 0, 0.3);
            color: #ffff00;
        }

        .action-btn.edit:hover {
            background: rgba(255, 255, 0, 0.2);
            box-shadow: 0 4px 15px rgba(255, 255, 0, 0.3);
        }

        .action-btn.delete {
            background: rgba(255, 0, 0, 0.1);
            border-color: rgba(255, 0, 0, 0.3);
            color: #ff4444;
        }

        .action-btn.delete:hover {
            background: rgba(255, 0, 0, 0.2);
            box-shadow: 0 4px 15px rgba(255, 0, 0, 0.3);
        }

        /* Divider */
        .divider {
            height: 2px;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 255, 0.5), transparent);
            margin: 3rem 0;
            border: none;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }

            .welcome-message {
                text-align: center;
            }

            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
            }

            .nav-links a {
                padding: 0.5rem 1rem;
                font-size: 0.9rem;
            }

            .container {
                padding: 1rem;
            }

            .form-grid {
                grid-template-columns: 1fr;
            }

            .section {
                padding: 1.5rem;
            }

            .table-container {
                font-size: 0.9rem;
            }

            th, td {
                padding: 0.7rem;
            }
        }

        @media (max-width: 480px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
            }

            .nav-links {
                flex-direction: column;
                width: 100%;
                gap: 0.5rem;
            }

            .nav-links a {
                width: 100%;
                text-align: center;
            }

            .section h2 {
                font-size: 1.5rem;
            }

            .action-btn {
                display: block;
                margin: 0.2rem 0;
            }
        }

        /* Background decoration */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 25% 25%, rgba(0, 255, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(255, 0, 255, 0.1) 0%, transparent 50%);
            pointer-events: none;
            z-index: -1;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .section {
            animation: fadeIn 0.6s ease-out;
        }

        /* Focus states for accessibility */
        .nav-links a:focus,
        .submit-btn:focus,
        .action-btn:focus,
        .logout-btn:focus {
            outline: 2px solid rgba(0, 255, 255, 0.5);
            outline-offset: 2px;
        }
    </style>
</head>

<body>
    <header>
        <div class="header-content">
            <div class="logo">BangBank</div>
            <div class="welcome-message">Welcome, <%= user.username %> (<%= user.role %>)</div>
            <nav class="nav-links">
                <a href="/admin/products">Manage Products</a>
                <a href="/admin/accounts">Account Applications</a>
                <a href="/admin/creditcards">Credit Card Applications</a>
                <a href="/profile">Profile Settings</a>
                <a href="/logout" class="logout-btn nav-logout-link">Logout</a>
            </nav>
        </div>
    </header>

    <div class="container">
        <!-- Success / Error Messages -->
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="message success">
                <%= success %>
            </div>
        <% } %>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="message error">
                <%= error %>
            </div>
        <% } %>

        <!-- Create Account Section -->
        <div class="section">
            <h2>Create New Account</h2>
            <form action="/admin/create" method="POST" enctype="multipart/form-data">
                <div class="form-grid">
                    <div class="form-group">
                        <label>Username:</label>
                        <input type="text" name="username" required>
                    </div>

                    <div class="form-group">
                        <label>Email:</label>
                        <input type="email" name="userEmail" required>
                    </div>

                    <div class="form-group">
                        <label>Password:</label>
                        <input type="password" name="userPassword" required>
                    </div>

                    <div class="form-group">
                        <label>First Name:</label>
                        <input type="text" name="first_name" required>
                    </div>

                    <div class="form-group">
                        <label>Last Name:</label>
                        <input type="text" name="last_name" required>
                    </div>

                    <div class="form-group">
                        <label>Alias (optional):</label>
                        <input type="text" name="alias">
                    </div>

                    <div class="form-group">
                        <label>Date of Birth:</label>
                        <input type="date" name="date_of_birth" required>
                    </div>

                    <div class="form-group">
                        <label>Phone Number:</label>
                        <input type="text" name="phone_number" required>
                    </div>

                    <div class="form-group">
                        <label>Country:</label>
                        <input type="text" name="country" required>
                    </div>

                    <div class="form-group">
                        <label>Address Line 1:</label>
                        <input type="text" name="address_line_1" required>
                    </div>

                    <div class="form-group">
                        <label>Address Line 2 (optional):</label>
                        <input type="text" name="address_line_2">
                    </div>

                    <div class="form-group">
                        <label for="postcode">Postal Code:</label>
                        <input type="text" name="postcode" required pattern="\d{6}" 
                               title="Postal code must be exactly 6 digits">
                    </div>

                    <div class="form-group">
                        <label>Profile Image:</label>
                        <input type="file" name="userImage" accept="image/*">
                    </div>

                    <div class="form-group">
                        <label>Role:</label>
                        <select name="userRole" required>
                            <% if (user.role === 'super_admin') { %>
                                <option value="admin">Admin</option>
                                <option value="advisor">Advisor</option>
                                <option value="user">Customer</option>
                            <% } else { %>
                                <option value="user">Customer</option>
                                <option value="advisor">Advisor</option>
                            <% } %>
                        </select>
                    </div>
                </div>

                <button type="submit" class="submit-btn">Create Account</button>
            </form>
        </div>

        <hr class="divider">

        <!-- Admin Accounts (super admin only) -->
        <% if (user.role === 'super_admin') { %>
            <div class="section">
                <h2>Admin Accounts</h2>
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.filter(u => u.userRole === 'admin').forEach(admin => { %>
                                <tr>
                                    <td><%= admin.username %></td>
                                    <td><%= admin.userEmail %></td>
                                    <td>
                                        <a href="/admin/details/<%= admin.userId %>" class="action-btn details">Details</a>
                                        <a href="/admin/edit/<%= admin.userId %>" class="action-btn edit">Edit</a>
                                        <a href="/admin/delete/<%= admin.userId %>" class="action-btn delete" 
                                           onclick="return confirm('Delete this admin?')">Delete</a>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
        <% } %>

        <!-- Advisor Accounts -->
        <div class="section">
            <h2>Advisor Accounts</h2>
            <div class="table-container">
                <table>
                    <thead>
                        <tr>
                            <th>Username</th>
                            <th>Email</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.filter(u => u.userRole === 'advisor').forEach(advisor => { %>
                            <tr>
                                <td><%= advisor.username %></td>
                                <td><%= advisor.userEmail %></td>
                                <td>
                                    <a href="/admin/details/<%= advisor.userId %>" class="action-btn details">Details</a>
                                    <a href="/admin/edit/<%= advisor.userId %>" class="action-btn edit">Edit</a>
                                    <a href="/admin/delete/<%= advisor.userId %>" class="action-btn delete" 
                                       onclick="return confirm('Delete this advisor?')">Delete</a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Customer Accounts -->
        <div class="section">
            <h2>Customer Accounts</h2>
            <div class="table-container">
                <table>
                    <thead>
                        <tr>
                            <th>Username</th>
                            <th>Email</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.filter(u => u.userRole === 'user').forEach(customer => { %>
                            <tr>
                                <td><%= customer.username %></td>
                                <td><%= customer.userEmail %></td>
                                <td>
                                    <a href="/admin/details/<%= customer.userId %>" class="action-btn details">Details</a>
                                    <a href="/admin/edit/<%= customer.userId %>" class="action-btn edit">Edit</a>
                                    <a href="/admin/delete/<%= customer.userId %>" class="action-btn delete" 
                                       onclick="return confirm('Delete this customer?')">Delete</a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book a Video Consultation - BangBank</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #e0e0e0;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            min-height: 100vh;
        }

        /* Header styles */
        header {
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 255, 255, 0.2);
            padding: 1rem 2rem;
            box-shadow: 0 2px 20px rgba(0, 255, 255, 0.1);
            position: relative;
            z-index: 1000;
        }

        .header-content {
            max-width: 1500px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 5rem;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(135deg, #00ffff, #ff00ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
            flex-shrink: 0;
        }

        .welcome-message {
            font-size: 1.2rem;
            color: #ffffff;
            text-shadow: 0 0 10px rgba(0, 255, 255, 0.3);
            flex-grow: 1;
            text-align: center;
        }

        .nav-links {
            display: flex;
            gap: 1rem;
            flex-shrink: 0;
        }

        .nav-links a {
            color: #e0e0e0;
            text-decoration: none;
            padding: 0.75rem 1.5rem;
            border-radius: 25px;
            background: rgba(0, 255, 255, 0.1);
            border: 1px solid rgba(0, 255, 255, 0.3);
            transition: all 0.3s ease;
            font-weight: 500;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
        }

        .nav-links a:hover {
            color: #00ffff;
            background: rgba(0, 255, 255, 0.2);
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
            border-color: rgba(0, 255, 255, 0.5);
        }

        /* Dropdown styles */
        .dropdown {
            position: relative;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            right: 0;
            top: 100%;
            background: rgba(0, 0, 0, 0.85);
            border: 1px solid rgba(0, 255, 255, 0.3);
            backdrop-filter: blur(8px);
            padding: 0.5rem 1rem;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0, 255, 255, 0.1);
            z-index: 999;
            min-width: 200px;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .dropdown-content a {
            display: block;
            padding: 0.5rem 0;
            color: #e0e0e0;
            text-decoration: none;
            border-bottom: 1px solid rgba(0, 255, 255, 0.1);
        }

        .dropdown-content a:last-child {
            border-bottom: none;
        }

        .dropdown-content a:hover {
            color: #00ffff;
            background: rgba(0, 255, 255, 0.1);
        }

        .nav-item {
            position: relative;
        }

        .dropdown-toggle {
            color: #e0e0e0;
            text-decoration: none;
            padding: 0.75rem 1.5rem;
            border-radius: 25px;
            background: rgba(0, 255, 255, 0.1);
            border: 1px solid rgba(0, 255, 255, 0.3);
            transition: all 0.3s ease;
            font-weight: 500;
            box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
            display: inline-block;
        }

        .dropdown-toggle:hover {
            color: #00ffff;
            background: rgba(0, 255, 255, 0.2);
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(0, 255, 255, 0.4);
            border-color: rgba(0, 255, 255, 0.5);
        }

        .dropdown-content {
            display: none;
            position: absolute;
            top: 120%; /* ensures space between button and menu */
            right: 0;
            background: rgba(0, 0, 0, 0.85);
            border: 1px solid rgba(0, 255, 255, 0.3);
            border-radius: 10px;
            padding: 0.5rem 0.75rem;
            box-shadow: 0 5px 20px rgba(0, 255, 255, 0.1);
            min-width: 220px;
            z-index: 100;
            backdrop-filter: blur(8px);
        }

        .nav-item:hover .dropdown-content {
            display: block;
        }

        .dropdown-content a {
            display: block;
            color: #e0e0e0;
            padding: 0.5rem 1rem;
            border-bottom: 1px solid rgba(0, 255, 255, 0.1);
            text-decoration: none;
        }

        .dropdown-content a:last-child {
            border-bottom: none;
        }

        .dropdown-content a:hover {
            background: rgba(0, 255, 255, 0.1);
            color: #00ffff;
        }

        .dropdown-content a {
            display: block;
            padding: 0.75rem 1rem; /* increased vertical padding */
            color: #e0e0e0;
            text-decoration: none;
            border-radius: 8px; /* adds pill feel */
            margin-bottom: 0.5rem; /* space between items */
            background: rgba(0, 255, 255, 0.05);
            transition: all 0.3s ease;
        }

        .dropdown-content a:last-child {
            margin-bottom: 0;
        }

        .dropdown-content a:hover {
            background: rgba(0, 255, 255, 0.15);
            color: #00ffff;
            transform: translateX(4px);
        }

        /* Main container */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        /* Page title */
        .page-title {
            color: #00ffff;
            font-size: 2.5rem;
            font-weight: 700;
            margin: 2rem 0;
            text-align: center;
            text-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
            background: linear-gradient(135deg, #00ffff, #ff00ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        /* Success message */
        .success-message {
            background: rgba(0, 255, 0, 0.1);
            border: 1px solid rgba(0, 255, 0, 0.3);
            color: #00ff00;
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            text-align: center;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 15px rgba(0, 255, 0, 0.1);
        }

        /* Error message */
        .error-message {
            background: rgba(255, 0, 0, 0.1);
            border: 1px solid rgba(255, 0, 0, 0.3);
            color: #ff4757;
            padding: 1rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            text-align: center;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 15px rgba(255, 0, 0, 0.1);
        }

        /* Section styles */
        section {
            margin-bottom: 3rem;
        }

        .section-header {
            color: #00ffff;
            font-size: 1.8rem;
            font-weight: 600;
            margin: 2rem 0 1rem 0;
            text-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
            text-align: center;
        }

        /* Empty state */
        .empty-state {
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(15px);
            padding: 2rem;
            border-radius: 15px;
            border: 1px solid rgba(0, 255, 255, 0.3);
            box-shadow: 0 8px 25px rgba(0, 255, 255, 0.1);
            text-align: center;
            color: rgba(224, 224, 224, 0.8);
        }

        /* Table styles */
        .table-container {
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid rgba(0, 255, 255, 0.3);
            box-shadow: 0 8px 25px rgba(0, 255, 255, 0.1);
            margin-bottom: 2rem;
            overflow: hidden;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th {
            background: rgba(0, 255, 255, 0.1);
            color: #00ffff;
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            text-shadow: 0 0 5px rgba(0, 255, 255, 0.3);
            border-bottom: 1px solid rgba(0, 255, 255, 0.2);
        }

        td {
            padding: 1rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            color: #e0e0e0;
        }

        tr:hover {
            background: rgba(0, 255, 255, 0.05);
        }

        tr:last-child td {
            border-bottom: none;
        }

        /* Status indicators */
        .status-booked {
            color: #00ff00;
            font-weight: 600;
            text-shadow: 0 0 5px rgba(0, 255, 0, 0.5);
        }

        .status-cancelled {
            color: #ff4757;
            font-weight: 600;
            text-shadow: 0 0 5px rgba(255, 71, 87, 0.5);
        }

        .status-completed {
            color: #5352ed;
            font-weight: 600;
            text-shadow: 0 0 5px rgba(83, 82, 237, 0.5);
        }

        /* Button styles */
        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
            text-align: center;
            position: relative;
            overflow: hidden;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn-primary {
            background: linear-gradient(135deg, #00ffff, #ff00ff);
            color: #000000;
            box-shadow: 0 6px 20px rgba(0, 255, 255, 0.4);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 255, 255, 0.6);
        }

        .btn-danger {
            background: linear-gradient(135deg, #ff4757, #ff3838);
            color: #ffffff;
            box-shadow: 0 6px 20px rgba(255, 71, 87, 0.4);
        }

        .btn-danger:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 71, 87, 0.6);
        }

        /* Bottom navigation */
        .bottom-nav {
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid rgba(0, 255, 255, 0.3);
            padding: 2rem;
            margin-top: 2rem;
            text-align: center;
            box-shadow: 0 8px 25px rgba(0, 255, 255, 0.1);
        }

        .bottom-nav a {
            color: #00ffff;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            margin: 0 1rem;
        }

        .bottom-nav a:hover {
            color: #ffffff;
            text-shadow: 0 0 10px rgba(0, 255, 255, 0.5);
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }

            .welcome-message {
                text-align: center;
            }

            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
            }

            .nav-links a {
                padding: 0.5rem 1rem;
                font-size: 0.9rem;
            }

            .container {
                padding: 1rem;
            }

            .table-container {
                overflow-x: auto;
            }

            table {
                min-width: 800px;
            }

            .page-title {
                font-size: 2rem;
            }
        }

        /* Background decoration */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 25% 25%, rgba(0, 255, 255, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(255, 0, 255, 0.1) 0%, transparent 50%);
            pointer-events: none;
            z-index: -1;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        } 

        .container > * {
            animation: fadeIn 0.6s ease-out;
        }

        /* Focus states for accessibility */
        .btn:focus,
        .nav-links a:focus {
            outline: 2px solid rgba(0, 255, 255, 0.5);
            outline-offset: 2px;
        }

        .logout-btn {
      background: linear-gradient(135deg, rgb(229, 10, 10), #ff4444) !important;
      color: #ffffff !important;
      border: none !important;
      padding: 0.75rem 1.5rem;
      border-radius: 25px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(255, 0, 255, 0.3);
      letter-spacing: 0.5px;
      font-size: 0.85rem;
      text-decoration: none;
    }

    .logout-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 0, 255, 0.5);
      background: linear-gradient(135deg, #ff3300, #ff6666) !important;
    }
    </style>
</head>

<body>
    <header>
        <div class="header-content">
            <div class="logo">BangBank</div>
            <div class="welcome-message">Welcome, <%= user.username %>!</div>
<nav class="nav-links">
  <a href="/user/dashboard">Back to Dashboard</a>
<a href="/logout" class="logout-btn">Logout</a>

</nav>


        </div>
    </header>

    <div class="container">
        <h1 class="page-title">Book a Video Consultation</h1>

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="success-message"><%= success %></div>
        <% } %>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>

        <!-- Available Sessions -->
        <section>
            <h2 class="section-header">Available Sessions</h2>

            <% if (sessions.length === 0) { %>
                <div class="empty-state">
                    <p>No available sessions at the moment.</p>
                </div>
            <% } else { %>
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Start Time</th>
                                <th>End Time</th>
                                <th>Advisor Username</th>
                                <th>Advisor Email</th>
                                <th>Booked Count</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% sessions.forEach(session => { %>
                                <tr>
                                    <td><%= session.session_date %></td>
                                    <td><%= session.session_time %></td>
                                    <td><%= session.end_time %></td>
                                    <td><%= session.advisor_name %></td>
                                    <td><%= session.advisor_email %></td>
                                    <td><%= session.booking_count %></td>
                                    <td>
                                        <form action="/customer/consultations/book/<%= session.session_id %>" method="POST">
                                            <button type="submit" class="btn btn-primary">Book</button>
                                        </form>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } %>
        </section>

        <!-- Booked Consultations -->
        <% if (typeof myConsultations !== 'undefined') { %>
            <section>
                <h2 class="section-header">My Booked Consultations</h2>

                <% if (myConsultations.length === 0) { %>
                    <div class="empty-state">
                        <p>You have no booked consultations.</p>
                    </div>
                <% } else { %>
                    <div class="table-container">
                        <table>
                            <thead>
                                <tr>
                                    <th>Date</th>
                                    <th>Time</th>
                                    <th>Advisor Username</th>
                                    <th>Advisor Email</th>
                                    <th>Status</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% myConsultations.forEach(c => { %>
                                    <tr>
                                        <td><%= c.session_date %></td>
                                        <td><%= c.session_time %></td>
                                        <td><%= c.advisor_name %></td>
                                        <td><%= c.advisor_email %></td>
                                        <td>
                                            <span class="status-<%= c.status %>"><%= c.status %></span>
                                        </td>
                                        <td>
                                            <% if (c.status === 'booked') { %>
                                                <form action="/customer/consultations/cancel/<%= c.consultation_id %>" method="POST">
                                                    <button type="submit" class="btn btn-danger">Cancel</button>
                                                </form>
                                            <% } else { %>
                                                -
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } %>
            </section>
        <% } %>

    </div>
</body>

</html>